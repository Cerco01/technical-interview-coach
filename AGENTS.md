# Agent Instructions

## Project Skill

Load the following skill whenever the user asks to practice, study, review, resume, or evaluate a technical interview:

- `skills/technical-interview-coach/SKILL.md`

Treat `curriculum/topics.json`, `data/questions/*.jsonl`, and `schemas/*.schema.json` as canonical project data. Follow `docs/workflows.md` for mode-specific disclosure rules. Never expose rubric content or solution guidance early in interview mode.
